#include "LED.h" //quotations mean it will take the library from the project file, as for <> is for arduino library files


LED::LED(int pin){

this->pin = pin;
}

void LED::init(){
pinMode(pin, OUTPUT);

}

void LED::on(){

digitalWrite(pin,1);
}
void LED::off(){

digitalWrite(pin, 0);
}
